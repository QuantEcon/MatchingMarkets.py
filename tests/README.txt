To use these tests, put the python file in the same directory as the matchingmarkets package and run

    python [test_name_here]
    
alternatively, you can call the full test suite from the package using the command

    import matchingmarkets as mm
    mm.FullTest()
    
which will run the full suite of tests for you
